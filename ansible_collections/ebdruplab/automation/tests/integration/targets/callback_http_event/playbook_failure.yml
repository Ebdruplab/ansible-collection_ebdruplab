---
- name: HTTP failure integration play
  hosts: all
  gather_facts: false
  tasks:
    - name: Mark healthy hosts
      ansible.builtin.debug:
        msg: healthy

    - name: Trigger HTTP failure on secondary host
      ansible.builtin.fail:
        msg: callback_http_event integration failure
      when: inventory_hostname == 'http_host2'
