# SPDX-License-Identifier: MIT-0
---
- name: Deploy a Semaphore project
  hosts: localhost
  gather_facts: false
  vars_files:
    - vars.yml

  roles:
    - role: ebdruplab.semaphoreui.project_deploy
